# =============================================
# MeetMate Backend Environment Variables
# =============================================
# Rename this file to .env.local and fill in your values

# Environment: development | staging | production
ENV=development

# Database URL
# Local: postgresql+psycopg2://meetmate:meetmate@localhost:5433/meetmate
# Supabase: postgresql://postgres.xxx:password@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
# Railway: postgresql://postgres:password@containers-xxx.railway.app:5432/railway
DATABASE_URL=postgresql+psycopg2://meetmate:meetmate@localhost:5433/meetmate

# Gemini AI API Key (get from https://ai.google.dev/)
GEMINI_API_KEY=

# Groq API (https://console.groq.com/keys)
GROQ_API_KEY=your-groq-api-key
GROQ_MODEL=llama-3.3-70b-versatile

# Gemini Model  
# Options: gemini-2.5-flash-lite, gemini-1.5-flash, gemini-1.5-pro
GEMINI_MODEL=gemini-2.5-flash-lite

# AI Settings
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=2048

# Security
SECRET_KEY=your-secret-key-min-32-characters

# CORS Origins (comma-separated or "*" for all)
# Production: https://your-frontend.vercel.app,https://your-domain.com
CORS_ORIGINS=*

# Optional: OpenAI (if using OpenAI instead of Gemini)
OPENAI_API_KEY=

# =============================================
# VNPT SMARTVOICE (REALTIME STT)
# =============================================
# Leave blank until you receive credentials.
# You can either set SMARTVOICE_ACCESS_TOKEN directly,
# or provide SMARTVOICE_TOKEN_ID/SMARTVOICE_TOKEN_KEY plus SMARTVOICE_AUTH_URL.
# Endpoint format is host:port (no scheme), e.g. smartvoice.example.com:443

SMARTVOICE_GRPC_ENDPOINT=
SMARTVOICE_ACCESS_TOKEN=
SMARTVOICE_TOKEN_ID=
SMARTVOICE_TOKEN_KEY=
SMARTVOICE_AUTH_URL=
SMARTVOICE_MODEL=fast_streaming

# =============================================
# VNPT GOMEET (CONTROL API)
# =============================================
# GoMeet API base for StartNewMeeting/Join
GOMEET_API_BASE_URL=https://gomesainterk06.vnpt.vn/api/v1
# Partner token for Authorization: Bearer <token>
GOMEET_PARTNER_TOKEN=
# Request timeout (seconds)
GOMEET_TIMEOUT_SECONDS=15

# =============================================
# EMAIL CONFIGURATION (Gmail SMTP)
# =============================================
# To send meeting minutes via email, configure these settings:

# Enable email sending (set to true when configured)
EMAIL_ENABLED=false

# SMTP Settings (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Your Gmail address
SMTP_USER=your-email@gmail.com

# Gmail App Password (NOT your regular password!)
# Generate at: https://myaccount.google.com/apppasswords
# Steps:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification if not already
# 3. Go to https://myaccount.google.com/apppasswords
# 4. Select "Mail" and your device
# 5. Copy the 16-character password (remove spaces)
SMTP_PASSWORD=your-app-password-here

# Display name for sent emails
EMAIL_FROM_NAME=MeetMate AI

# =============================================
# Supabase Storage (S3-compatible)
# =============================================
# Endpoint is shown in Supabase Storage > S3 Configuration (e.g., https://xxxx.supabase.co/storage/v1/s3)
SUPABASE_S3_ENDPOINT=
SUPABASE_S3_REGION=ap-southeast-1
SUPABASE_S3_BUCKET=
SUPABASE_S3_ACCESS_KEY=
SUPABASE_S3_SECRET_KEY=
